adigit.exe - part of Cognitive Framework 
For support trains2 module. 

Accepts as an input semantic database XML with digits of numbers of a train (8 digits for each number)
and produces XML file with 8 rectagles, wich represents average positions for each digit in the bounding box.

Output File is Average.xml - the TLFSemanticImageDescriptor
